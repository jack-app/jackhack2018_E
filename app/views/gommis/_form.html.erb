<%= form_with(model: gommi, local: true) do |form| %>
  <% if gommi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gommi.errors.count, "error") %> prohibited this gommi from being saved:</h2>

      <ul>
      <% gommi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :explanation %>
    <%= form.text_field :explanation %>
  </div>

  <div class="field">
    <%= form.label :kind %>
    <%= form.text_field :kind %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
